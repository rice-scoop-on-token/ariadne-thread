# AGENTS.md

## 프로젝트

**ariadne-thread** — 아이디에이션 브레인스토밍 시각화 서비스.

작업 전 다음 문서를 읽는다.

- `docs/ux-flow.md` — UX 흐름 명세. 단계 ID(P0~P8)가 모든 논의의 기준이다
- `docs/architecture.md` — 백엔드 설계 및 레포 구조

## 작업 단위

- **한 번에 하나의 커밋 범위만 작업한다.** 여러 커밋에 걸칠 작업은 시작하지 않는다
- 작업 시작 전 이번 커밋의 범위와 수정 대상 파일을 먼저 제시하고 승인을 받는다
- 범위를 벗어나는 개선점은 그 자리에서 수정하지 않고 다음 커밋 후보로 기록한다

## Git

- **직접 커밋·푸시·브랜치 생성·머지를 금지한다.** 커밋 메시지 제안까지만 수행한다
- `git add`를 포함한 스테이징도 수행하지 않는다
- 커밋 메시지는 Conventional Commits 형식을 따른다

## 코드 규칙

- **스택을 임의로 추가·교체하지 않는다.** 새 의존성이 필요하면 이유와 함께 먼저 제안한다
- `backend/src/domain/` 은 I/O를 포함하지 않는다. DB·네트워크·파일 접근을 금지한다
- 모듈 최상위에서 DB 클라이언트나 Redis 인스턴스를 직접 import하지 않는다. 의존성은 `container.ts` 에서 주입한다
- 요청·응답·WebSocket 이벤트 타입은 `packages/contracts` 에만 정의한다. backend·frontend에 중복 정의하지 않는다
- 발산 화면(`Round.tsx`, `features/ideation/**`)에서 `features/canvas/**` 를 import하지 않는다. 고착 방지 설계의 핵심이므로 린트 규칙으로 강제한다

## 응답

- 문서·주석·커밋 메시지는 한국어로 작성한다
- 구현하지 않은 것을 완료했다고 보고하지 않는다
